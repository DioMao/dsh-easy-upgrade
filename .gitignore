# Build artifacts
lib/

# Dependencies
/node_modules

# Tooling is supplied by the selected DeepSeek Harness checkout; this package
# deliberately has no independent dependency graph or lockfile.
pnpm-lock.yaml

# Local checks and package previews.
.npm-cache/

# Internal Aegis/dev tracking (not plugin source)
docs/

# Local tooling scratch
*.tsbuildinfo
*.log
.DS_Store
*.tmp

.scratch/
