# Build artifacts
lib/

# Dependencies
/node_modules

# Standalone package has no install; pnpm auto-emits a lockfile when its scripts run.
pnpm-lock.yaml

# Internal Aegis/dev tracking (not plugin source)
docs/

# Local tooling scratch
*.tsbuildinfo
*.log
.DS_Store
*.tmp
